# {project_name} `.context` 使用说明

`.context/` 是本项目的工程操作系统。开始真实实施前先读这里，再读 `master-plan.md`、`current-status.md`、`task-breakdown.md`。

## 使用原则

1. 先更新上下文，再继续实施。
2. 每完成一个明确步骤，追加 `work-log.md`。
3. 每遇到问题，记录 `bug-log.md`。
4. 每做出高影响决策，更新 `decisions.md`。
5. 每次会话结束前，刷新 `current-status.md`。
